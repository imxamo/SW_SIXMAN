http://우리주소.com/get
에 누군가 접속하면 200이라고 대답해야한다.
하지만 만약 최근에 프론트에서 사진 찍기 버튼이 눌렸다면 201을 전송한다.

if(camActive == 1){
  // 201이라고 전송한다.
  camActive = 0;
}
else 200이라고 전송한다.

http://우리주소.com/upload
이 주소로 누가 접속하면 CAM이 이미지를 보내올 것이다.
이미지를 저장해라.
